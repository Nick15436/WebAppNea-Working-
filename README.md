to see what's going on

    git status

to add all changes to the next commit

    git add -A

to commit all changes

    git commit -m "useful message"

to push to GitHub

    git push

to view all previous commits

    git log

When using git log:
* use j and k to scroll up/down
* use q to quit
